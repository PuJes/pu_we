# 个人网站 JESS.PU 完整产品与设计文档汇总

> 本文档自动合并自原 `docs` 目录下的散列说明文档，为了便于查阅构建为单一文件。

---

## 📚 目录概览

- **系统基础与技术架构**
  - [README.md](#readmemd)
  - [technical_architecture_specs.md](#technical_architecture_specsmd)
  - [cms_architecture_specs.md](#cms_architecture_specsmd)
- **全局设计规范与统一线框**
  - [design_system_v5_clean_grand.md](#design_system_v5_clean_grandmd)
  - [all_pages_ui_wireframes.md](#all_pages_ui_wireframesmd)
- **主页 (Home)**
  - [home_product_specs_v2.md](#home_product_specs_v2md)
  - [home_ui_wireframe.md](#home_ui_wireframemd)
- **开放实验室 (Open Lab)**
  - [open_lab_product_specs.md](#open_lab_product_specsmd)
  - [open_lab_ui_wireframe.md](#open_lab_ui_wireframemd)
- **AI实验 (AI Experiments)**
  - [ai_experiments_product_specs.md](#ai_experiments_product_specsmd)
  - [ai_experiments_ui_wireframe.md](#ai_experiments_ui_wireframemd)
- **商业分析 (Business Analysis)**
  - [business_analysis_product_specs.md](#business_analysis_product_specsmd)
  - [business_analysis_ui_wireframe.md](#business_analysis_ui_wireframemd)
- **我的故事 (My Story)**
  - [my_story_product_specs.md](#my_story_product_specsmd)
  - [my_story_ui_wireframe.md](#my_story_ui_wireframemd)
- **独立详情页 (Posts)**
  - [post_detail_product_specs.md](#post_detail_product_specsmd)
- **订阅与授权 (Auth & Subscribe)**
  - [subscribe_auth_specs.md](#subscribe_auth_specsmd)
  - [subscribe_auth_ui_wireframe.md](#subscribe_auth_ui_wireframemd)

---


# ==================================================
# 🗂️ 模块：系统基础与技术架构
# ==================================================

## 📄 文档节点：README.md <a id="readmemd"></a>

# JESS.PU 个人站点产品需求与设计规划 (PRD & UI Wireframes)

本目录包含了基于“Build in Public (公开构建)”和“无密码认证”理念的个人独立站全套需求与交互线稿。
文档经过统一梳理，确保前端多栏排版（Bento Box / Two-Column）与底层 CMS 架构（Payload CMS）的高效解耦。

## 目录索引 (Index)

### 1. 全局架构与底层引擎
*   **`cms_architecture_specs.md`**：内容管理系统 (CMS) 架构说明。定义了“万物皆 Content”的底层表结构，如何通过一张大表支撑不同的专题，并包含评论/审核的统一管理模型。
*   **`subscribe_auth_specs.md`**：订阅与用户体系说明。抛弃传统密码，实行“邮箱 + OTP 验证码”的静默登录体系。
*   **`subscribe_auth_ui_wireframe.md`**：订阅页与免密认证弹窗交互线稿。包含打赏入口、微信二维码以及邮件订阅转化卡片。

### 2. 核心大盘
*   **`home_product_specs.md`**：首页需求说明。作为站点的总枢纽，采用高信息密度的数字仪表盘展示个人价值观。
*   **`home_ui_wireframe.md`**：首页 UI 交互线稿。采用大胆的 **Bento Box (便当盒)** 风格拼图式排版，收口四大核心能力。

### 3. 共创板块
*   **`open_lab_product_specs.md`**：公开实验室 (Open Lab) 需求说明。全站最具特色的互动模块，采用 `Idea (大灵感) -> Feature (具体功能点)` 的双层架构。
*   **`open_lab_ui_wireframe.md`**：公开实验室 UI 交互线稿。强调左侧 Builder 过程展示与右侧用户共创的“双轨并行”。

### 4. 核心内容专题 (Content Pillars)
*   **`ai_experiments_product_specs.md`** / **`ai_experiments_ui_wireframe.md`**：AI 创造与实验专题。采用双栏布局，核心亮点为“结论前置 (Takeaways)”，展示硬核实战复盘与工作流。
*   **`business_analysis_product_specs.md`** / **`business_analysis_ui_wireframe.md`**：商业分析专题。纯粹的观点阅读场，偏向严谨的投研研报排版，主打“核心论点 (Key Argument)”外显。
*   **`my_story_product_specs.md`** / **`my_story_ui_wireframe.md`**：我的故事专题。展示个人数字花园与生活记录，强调图文并茂的“碎碎念 (Snippet)”感性排版。

### 5. 详情消费页
*   **`post_detail_product_specs.md`**：文章详情页需求说明。承载单篇内容的消费，主推向 B站、小红书等外部社媒的流量回流与打赏转化。

---
**核心全局交互规范**：
在详情页、Open Lab 讨论区、专题侧边栏等高优曝光位，均植入轻量级的 `[ ☕️ 打赏 / Buy me a coffee ]` 入口，点击后统一弹出或跳转至 `/subscribe` 里的打赏卡片，以最顺滑的方式承接用户赞赏意愿。

11. **`technical_architecture_specs.md`**
    - 技术架构与数据流向设计说明。详细规定了 Next.js + Payload CMS 的表结构字段（Contents, Ideas, Features, Users, Votes）以及 SSR、OTP 登录、防并发点赞等关键业务的数据流向图。


---

## 📄 文档节点：technical_architecture_specs.md <a id="technical_architecture_specsmd"></a>

# JESS.PU 个人站点技术架构与数据流向设计说明

## 1. 整体架构选型 (Tech Stack)
本站点采用现代化的 **Headless CMS + 边缘渲染** 架构，确保极致的 SEO 表现、极快的首屏加载速度以及高度灵活的后台管理体验。

*   **前端框架**: Next.js 14+ (App Router 模式)。全站优先使用 React Server Components (RSC) 进行服务端渲染，仅在交互区（如点赞、评论表单）使用 Client Components。
*   **样式方案**: Tailwind CSS (配合 CSS Modules/Vanilla Extract 辅助实现极客风 Bento Box 排版)。
*   **后端/Headless CMS**: Payload CMS (v3, 基于 Next.js 原生集成，无需独立部署 Node.js 服务)。
*   **数据库**: PostgreSQL 或 MongoDB (通过 Payload 官方 Adapter 接入)。
*   **无密码认证**: 采用 OTP (One-Time Password) / Magic Link 方案。依赖 Nodemailer 或 Resend 发送验证码邮件，使用 JWT 在 HTTP-Only Cookie 中维持会话状态。
*   **部署环境**: Vercel (前端 + Serverless Functions) 或自有服务器 (Docker 容器化统一部署)。

---

## 2. 后端数据结构详细设计 (Payload CMS Collections)

为了支撑前台复杂的业务流转，底层数据表不仅要存储内容，还需要处理状态机和关系映射。

### 2.1 内容大表 `Contents`
支撑所有单向宣发的内容（AI 实验、商业分析、我的故事、Vlog）。

| 字段名 | 类型 | 说明与约束 |
| :--- | :--- | :--- |
| `id` | String | 主键 (UUID/CUID) |
| `title` | String | 必填，主标题 |
| `slug` | String | 必填，唯一，用于生成 SEO 友好的 URL (例如 `my-first-vlog`) |
| `type` | Select | 媒体类型：`article`, `video`, `podcast`, `repo`, `snippet` |
| `category` | Select | 归属专题：`ai-experiments`, `business-analysis`, `my-story` |
| `tags` | Array<String> | 可选，自由输入或关联 Tags 表 |
| `takeaways` | Array/Text | **条件字段**：当 `category` 为 `ai-experiments` 时显示 |
| `keyArgument` | Text | **条件字段**：当 `category` 为 `business-analysis` 时显示 |
| `analysisFramework`| String | **条件字段**：当 `category` 为 `business-analysis` 时显示 |
| `coverImage` | Upload | **条件字段**：当 `category` 为 `my-story` 时必填或强推荐 |
| `externalLink` | Text (URL) | 若填写，前端列表页/详情页的“阅读”按钮将直接外跳 |
| `body` | RichText | 正文（支持 Markdown, 代码块等） |
| `status` | Select | `draft` (草稿) \| `published` (已发布) |
| `publishedAt` | Date | 发布时间，用于前端瀑布流排序 |

### 2.2 灵感引擎表 `Ideas` (Open Lab 核心)
存储宏大的产品痛点和生命周期状态。

| 字段名 | 类型 | 说明与约束 |
| :--- | :--- | :--- |
| `id` | String | 主键 |
| `title` | String | 必填，灵感标题 (例如“独立开发者订阅管理工具”) |
| `description`| Text | 痛点描述 |
| `status` | Select | 生命周期状态机：<br>`pending`(待共鸣) -> `discussing`(讨论中) -> `approved`(已立项) -> `in-progress`(研发中) -> `launched`(已交付) |
| `priorityScore`| Number | 后端定时计算/更新的热度分 (公式：基础分 + 投票数 * 权重) |
| `voteCount` | Number | 冗余字段，缓存当前总点赞数，提升读取性能 |
| `builderLogs` | Array<Block> | **核心**：开发进度快照流。包含：`date`, `content`(文本/图片), `version`(关联版本号) |

### 2.3 功能共创表 `Features` (子需求)
依附于特定的 `Idea`，由用户提出具体的落地功能建议。

| 字段名 | 类型 | 说明与约束 |
| :--- | :--- | :--- |
| `id` | String | 主键 |
| `idea` | Relationship | 必填，关联到 `Ideas` 表中的某个 ID |
| `author` | Relationship | 必填，关联到 `Users` 表 (提出该功能的极客) |
| `content` | Text | 必填，功能建议描述 (如“希望能接入 Stripe API”) |
| `voteCount` | Number | 缓存该功能的获赞数 |
| `builderReply`| Text | 选填，站长对该建议的明确回复 (如“安排在 v1.1”) |

### 2.4 用户中心表 `Users` (无密码体系)
管理订阅者和共创者身份。

| 字段名 | 类型 | 说明与约束 |
| :--- | :--- | :--- |
| `id` | String | 主键 |
| `email` | String | 必填，唯一索引 (系统将通过 OTP 验证该邮箱) |
| `nickname` | String | 选填，用户称呼，默认随机分配 (如 `Geek_0A4B`) |
| `role` | Select | `admin` (站长，拥有后台权限与前端专属高亮) \| `user` |
| `isSubscribed`| Boolean | 是否同意接收“内参周报”邮件，默认 false |
| `otpSecret` | String | 隐藏字段，临时存储下发的 6 位验证码，过期失效 |
| `otpExpiresAt`| Date | 隐藏字段，验证码过期时间 |

### 2.5 防刷票审计表 `Votes`
确保一人一票的核心基石。每次用户点击点赞，都会先查此表。

| 字段名 | 类型 | 说明与约束 |
| :--- | :--- | :--- |
| `id` | String | 主键 |
| `targetType` | Select | 投票对象类型：`idea` \| `feature` |
| `targetId` | String | 被投票的实体 ID |
| `userIdentifier`| String | 强验证：如果是登录用户，存 `User.id`；<br>弱验证(如未登录点赞)：存基于 IP + UserAgent 计算出的 Hash 值 |

---

## 3. 关键业务数据流向图 (Data Flow)

### 3.1 页面加载时的数据渲染 (SSR Flow)
以打开“商业分析专题 (`/analysis`)”为例：
1. **[Client]** 浏览器发起 GET 请求。
2. **[Next.js Server]** Server Component 接收到请求，直接在服务端调用 Payload 的 Local API：`payload.find({ collection: 'contents', where: { category: { equals: 'business-analysis' }, status: { equals: 'published' } } })`。
3. **[Next.js Server]** 拿到数据后，将数据注入到 Bento Box 卡片组件中，生成完整的 HTML HTML。
4. **[Client]** 浏览器接收到预渲染好的 HTML，瞬间完成首屏绘制（无需等待客户端 Fetch 请求）。

### 3.2 首次发帖与身份验证流 (OTP Auth Flow)
场景：未登录的访客在 Open Lab 某个 Idea 下面，想提出一个新 `Feature`。
1. **[Client]** 访客点击“提交建议”，前端发现本地无合法 Cookie，弹起 Auth Modal。
2. **[Client]** 访客填入邮箱 `test@gmail.com`，点击“获取验证码”。发起 POST `/api/auth/send-otp`。
3. **[Backend]** 检查 `Users` 表有无此邮箱；若无则创建 `pending` 状态的记录。生成 6 位随机码，更新 `otpSecret` 和 `otpExpiresAt`，调用 Resend 发送邮件。
4. **[Client]** 访客查收邮件，填入 6 位验证码，点击“确认并发布”。带着验证码和刚才的 Feature 文本发起 POST `/api/features/create-with-auth`。
5. **[Backend]** 校验验证码通过。
6. **[Backend]** 将该用户状态置为正式用户，签发 JWT 并写入 HTTP-Only Cookie。
7. **[Backend]** 在 `Features` 表中写入该条新功能建议，并将其 `author` 绑定为该用户。
8. **[Client]** 收到成功响应，自动关闭弹窗，刷新局部数据流，展示刚发布的建议。

### 3.3 异步点赞与高并发防护流 (Upvote Flow)
场景：用户在广场上点击“我也需要 (👍)”为某个 Idea 投票。
1. **[Client]** 拦截点击事件，按钮立刻执行“放大+数字+1”的乐观更新 (Optimistic UI) 动效，让用户感觉极度丝滑，同时静默发起 POST `/api/votes` 包含 `{ targetType: 'idea', targetId: '123' }`。
2. **[Backend]** 接口接收到请求，提取用户的 `userId` (如有 Cookie) 或计算 `IP+UA Hash`。
3. **[Backend Database]** 查询 `Votes` 表是否存在对应的 `(targetId, userIdentifier)` 组合。
4. **[Backend 分支 A：已存在]** 直接返回 409 或 200(但标识已投过)，不做任何变更。
5. **[Backend 分支 B：合法新票]**
    *   在 `Votes` 表中 `insert` 一条新记录。
    *   **核心性能优化**：不直接触发复杂的连表 count 查询，而是对 `Ideas` 表中的对应记录执行原子自增操作 `$inc: { voteCount: 1 }`。
6. **[Client]** 如果接口返回报错（比如提示已投过），撤销之前的乐观更新数字；如果成功，维持现状。

## 4. 部署与运维策略 (Deployment via Railway)

### 4.1 为什么选择 Railway + Docker？
本架构（Next.js + Payload CMS）非常适合使用 Railway 的容器化部署方案：
*   **一体化部署**：Payload v3 作为 Next.js 的路由集成，不需要拆分“前端服务器”和“后端管理台”。使用一个 `Dockerfile` 构建出一个 Next.js 镜像，即可同时包含前台页面和后台 CMS `/admin`。
*   **无缝衔接数据库**：Railway 提供开箱即用的 PostgreSQL (或 MongoDB) 插件，只需一键 Provision，将 `DATABASE_URI` 注入到应用的 Environment Variables 即可，免去了云数据库单独配置的烦恼。
*   **对象存储剥离**：既然部署在 Railway 这种无状态容器上（容器重启会导致本地文件丢失），**绝对不能把图片存入本地文件系统**。必须在 Payload 侧配置 S3 插件（如 AWS S3, Cloudflare R2, 或 Supabase R2），将 `coverImage` 等富媒体文件直接透传存储在对象存储中，使得 Railway 的容器成为完全“无状态 (Stateless)”的高可用节点。

### 4.2 扩展性评估 (Scalability & Modification)

本架构在设计之初就贯彻了“高内聚、低耦合”的原则，极易修改和扩展：

*   **新增内容板块（极易）**：
    所有内容数据都收敛在 `Contents` 集合中。如果未来你想增加一个“我的摄影展”板块，**不需要新建表**。只需要在 `category` 枚举中增加一个 `photography`，在前端路由 `/photography` 抓取这个分类的数据，并为其定制一种瀑布流的 UI 组件即可。数据层完全不动。
*   **修改表结构（自动化）**：
    如果需要在 `Ideas` 表里增加一个新字段（比如“预算估算”），只需在 Payload CMS 的 Schema (TypeScript) 里加一行代码，Payload 会在下次启动时自动进行 Database Migration（如果在 PostgreSQL 模式下）或者自动适应（MongoDB 模式下）。
*   **切换前端框架（解耦）**：
    虽然我们当前使用了 Next.js 与 Payload 同构部署，但 Payload 默认会为你生成一整套标准化的 REST / GraphQL API。如果你未来想用 React Native 开发一个手机 App，或者想用 Vue/Nuxt 重写前台页面，底层数据、用户系统和 CMS 都不需要动，直接调用现成的 API 即可。

### 4.3 澄清：Railway 数据持久化
关于 Railway 的数据存储，需要明确区分“数据库数据”和“媒体文件数据”：
*   **可以存储的数据 (PostgreSQL/MongoDB)**：文本内容、文章、评论、点赞数、用户表等结构化数据。你在 Railway 里一键开通的 Database 插件是**持久化**的，容器重启或重新部署都不会丢失数据。这部分完全交给 Railway 管理即可。
*   **不可以/不建议存储的数据 (Media/Images)**：你在后台上传的封面图、博客文章里的插图。在 Docker 容器内部，默认的文件系统是**临时**的（无状态）。如果你把图片传到容器的 `/public/media` 目录下，下一次你更新代码重新部署 Docker 镜像时，这些图片就会全部丢失。
*   **解决方案**：这也就是为什么需要接入 S3 对象存储（如 AWS S3、Cloudflare R2 或阿里云 OSS）。数据库（存文本）依然在 Railway，但当你上传一张图片时，Payload CMS 会自动把图片发到 S3，并在数据库里存一个 S3 的 URL。这样你的应用就变成了完美的无状态架构。


---

## 📄 文档节点：cms_architecture_specs.md <a id="cms_architecture_specsmd"></a>

# 内容管理系统 (CMS Architecture) 架构与需求说明

## 1. 系统定位与核心诉求
作为整个站点的底层引擎，CMS 需要统一支撑前台所有的专题模块（AI 创造、商业分析、我的故事）。
它不仅仅是一个简单的“博客后台”，而是一个**全媒体内容集散中心**与**社区交互管理台**。
*   **统一存储，多元展示**：所有内容（不论是文章、视频、Repo 还是简短的碎碎念）统一存在一张大表里，通过标签和属性控制前端不同专题的渲染。
*   **富交互管理**：除了发内容，还要能方便地管理用户的评论、点赞和 Open Lab 里的需求流转。
*   **引流优先**：支持站内原生排版的同时，原生支持向 B站、小红书、YouTube 等外部平台的强导流。

---

## 2. 核心数据集合设计 (Collections)
基于 Payload CMS（或类似的 Headless CMS），系统应包含以下核心集合 (Collections)：

### 2.1 统一内容大表 (`Contents`)
所有在前台流转的信息（文章、视频、播客）都存在这里。
*   `title` (标题): 必需。
*   `type` (媒体类型枚举): `article` | `video` | `podcast` | `repo` | `snippet`(短动态)。
*   `category` (所属专题): `ai-experiments` | `business-analysis` | `my-story`。
*   `tags` (二级标签): 数组，如 `[Vibe Coding]`, `[Prompt]`, `[$TCEHY]`, `[Vlog]`。
*   **【专题专属字段 (Conditional Fields)】**：根据 `category` 的不同，后台动态显示不同的字段，确保填写的结构化：
    *   如果是 `ai-experiments`: 显示 `takeaways` (核心干货提炼)。
    *   如果是 `business-analysis`: 显示 `keyArgument` (核心论点) 和 `framework` (分析框架)。
    *   如果是 `my-story`: 显示 `coverImage` (大封面图) 和 `snippet` (感性碎碎念)。
*   `externalLink` (外部跳转链接): 若配置，前端点击卡片或按钮将直接跳出（B站/小红书等）。
*   `body` (站内富文本): 选用块级编辑器 (Block Editor) 支持 Markdown、代码高亮、多媒体嵌入。
*   `status`: `draft` | `published`。
*   `publishedAt`: 发布时间。

### 2.2 评论与互动大表 (`Comments`)
统一管理全站所有的 UGC (用户生成内容)。
*   `content` (富文本/Markdown): 用户评论的内容。
*   `author`: 关联 `Users` 集合（已登录），或简单的 `guestName` (游客昵称) + `guestEmail`。
*   `targetType` (评论目标类型): `content` (文章/视频评论) | `idea` (Open Lab 大灵感评论) | `feature` (Open Lab 具体功能评论)。
*   `targetId`: 关联到具体的 `Contents`, `Ideas`, 或 `Features` 的 ID。
*   `status` (审核状态): `pending` (待审核) | `approved` (已通过，前端可见) | `rejected` (拒绝)。
*   `upvotes`: 点赞数统计。

### 2.3 实验室流转引擎 (`Ideas` & `Features`)
(详见 `open_lab_product_specs.md`)
*   `Ideas`: 管理大概念。字段包括标题、痛点描述、所处阶段 (`pending` -> `in-progress` -> `launched`)、总票数、Builder 的研发日志 (Timeline 数组)。
*   `Features`: 挂载在特定 `Idea` 下的具体功能点。包含提议内容、票数、Builder 回复。

### 2.4 投票防刷记录 (`Votes`)
用于记录用户的每一次点赞（文章点赞、Idea 点赞、Feature 点赞、评论点赞）。
*   `targetType` & `targetId`: 投票对象。
*   `userIdentifier`: 登录用户的 ID 或未登录用户的设备指纹/IP Hash（防刷票）。

---

## 3. CMS 后台管理体验 (Admin UI)

### 3.1 仪表盘 (Dashboard)
*   进入 CMS 第一眼看到的是“待办事项”：
    *   X 条待审核的新评论。
    *   X 个新提交的 Open Lab Idea。
    *   X 个票数超过阈值亟待评估的 Feature。

### 3.2 内容发布体验 (Content Authoring)
*   **按需显示**：在创建新 `Content` 时，选择 `商业分析` 分类后，页面自动弹出 `核心论点` 输入框；选择 `我的故事` 后，自动要求上传 `封面大图`。
*   **快捷外链**：提供一键抓取外部链接 Meta 信息的能力（未来优化方向，输入 B 站链接自动补全标题和封面）。

### 3.3 审核与互动工作流 (Moderation)
*   提供一个专门的“互动中心 (Engagement)”视图。
*   管理员可以批量勾选 `approved` 评论。
*   可以在后台直接以 `[Admin/Builder]` 身份回复评论，前端会带有专属高亮徽章展示。


---


# ==================================================
# 🗂️ 模块：全局设计规范与统一线框
# ==================================================

## 📄 文档节点：design_system_v5_clean_grand.md <a id="design_system_v5_clean_grandmd"></a>

# JESS.PU 个人站点 UI/UX 设计规范 (V5 Clean & Grand)

## 一、 核心定位与视觉概念 (Core Vision)

**“简洁无垠，大气磅礴” (Clean & Grand)**

在坚决摒弃了厚重的深色背景与繁杂的刻板“黑客风”之后，我们采用了极其纯净的**现代主义艺术画廊 (Modernist Gallery)** 风格。
通过近乎极致的“留白 (Negative Space)”与“巨型字体 (Oversized Typography)”的张力对比，在不用任何黑色背景块的前提下，依然能实现“大开大合”的高阶质感。它看起来应该像是一本极其精美的纸质高级独立杂志或建筑图册。

### 设计关键词 (Keywords)
*   **Breathing Room (绝对的呼吸感)**：用超大的内边距 (Padding) 代替背景色来划分区域。
*   **Monochrome Elegance (优雅单色)**：除了极其克制的单一品牌点缀色外，90% 的页面由纯白、极浅灰与碳黑文字组成。
*   **Typographic Tension (排版张力)**：让文字本身成为最大的装饰，用极其巨大的首屏标题镇住整个气场。

---

## 二、 版式架构体系 (Layout Architecture)

### 2.1 The Grand Bento (首页超级白盒网格)
*   继承了 Bento Box 的分块逻辑，但在全浅色下呈现。
*   **无缝衔接**：整个网页底色为极浅的冷灰 `#F9FAFB`。网格内的卡片为纯白 `#FFFFFF`。
*   **卡片边缘处理**：抛弃刺眼的粗黑线，采用极细的 `1px solid #E5E7EB` (Tailwind 的 `gray-200`)，或者在底部添加几乎察觉不到的弥散阴影 `box-shadow: 0 4px 24px -4px rgba(0,0,0,0.03)`，让纯白卡片在浅灰底上优雅地浮起，就像纸张叠在一起。

### 2.2 跨页面的版式对齐
无论是两栏布局的文章详情，还是 Open Lab 讨论区，都必须遵守统一的左对齐网格基准线。视觉引导线清晰，绝不拖泥带水。

---

## 三、 设计 Token 系统 (Design Tokens)

V5 版本是对系统调色板的彻底“净化”。**严禁使用任何形式的大面积纯黑、纯深灰色块做容器背景。**

### 3.1 Color Tokens (极简调色板)
*   `--bg-body-base`: `#F8F9FA` 或 `#F3F4F6` (最底层的画布，带来极其轻微的光影对比)。
*   `--bg-card-surface`: `#FFFFFF` (所有的内容模块、Bento 卡片永远是纯白色)。
*   `--border-subtle`: `#E5E7EB` (用于所有的卡片边框、内容分割线，保证隐形而不争抢注意力)。
*   `--text-primary`: `#111827` (碳黑，而非死黑 `#000`，使长时间阅读不觉刺眼)。
*   `--text-secondary`: `#6B7280` (中灰，用于副标题、日期、辅助类元信息)。
*   `--brand-accent`: 视您的偏好定（例如冷静克制的深空蓝 `#1D4ED8` ），只允许出现在最重要的 CTA 按钮或文本悬停高亮上。

### 3.2 告别异构，走向和谐统一
*   **AI 板块**：不再是黑客终端！而是一张纯白卡片，通过使用带有代码片段的精美插图，或仅仅通过等宽字体排版来暗示其属性。
*   **商业板块**：纯净的白纸黑字布局。
*   无论哪个板块，基础容器的质感 (白底+细边框) 保持绝对的一致。

---

## 四、 排印层次 (Typography System)

在色彩被极其克制的情况下，“大字”和有品位的字体选择是体现“大气”的唯一途径。

### 4.1 超大无衬线标题 (Oversized Clean Display)
*   全站的主标题 (H1)、关键数字指标，均采用几何感明确的现代无衬线体（推荐 `Inter Tight`, `Space Grotesk`, 或直接使用系统 UI 字体 `system-ui` 黑体）。
*   尺寸要求“突破常规”：Hero 区域的标题可以是 `clamp(3.5rem, 8vw, 7rem)`。要让用户一进网页，首先被巨大的文字构成所震撼。
*   字重(Font-Weight)：采用 `600` (Semibold) 或更重，形成扎实的块状感。

### 4.2 段落与正文 (Body & Detail)
*   干净清晰的 `Inter` 或 `SF Pro`。
*   为了防止文字显得拥挤，统一使用 `leading-relaxed` (1.625 行高) 甚至 `leading-loose` (2 行高)，进一步烘托空灵感。

---

## 五、 组件与微交互 (Components & Motion)

### 5.1 极简按钮 (Ghost & Monolithic Buttons)
*   **Primary CTA (主要按钮)**：纯黑底色 (`#111827`)，纯白文字。四角圆润但不过度 (`rounded-lg` 或完全的胶囊 `rounded-full`)。这可能是全站唯一合法的纯黑“大色块”，正因为稀少所以极具引导性。
*   **Secondary CTA (次要按钮 / 打赏入)**：完全融入背景的白底按钮，仅靠 `1px` 细框和极轻微的 `gray-50` 悬浮反馈界定。

### 5.2 微交互原则 (Fluid & Breathable Motion)
*   动画需要像丝绸一样滑润顺畅。
*   **Hover 滑动**：当鼠标掠过卡片时，不要加重边框线条，而是微微增大阴影（如提升到 `shadow-md`）并配合极致平滑的 Y 轴上浮 `-2px`，转场时长 `duration-300 ease-out`。
*   **页面切换**：优雅的整体淡入 (Fade-in)。

---

## 工程师执行要点 (Checklist for Dev)

1.  **清空 `tailwind.config.js` 中的暗黑相关定制**，不需要 `darkMode`。
2.  全页面的 `<body>` 使用 `bg-gray-50`，所有 Bento 卡片组件一律使用 `bg-white shadow-sm border border-gray-200 rounded-2xl` 的基准组合。
3.  确保 H1 到 H3 都配有对应的 `tracking-tight` (紧凑字间距) 设置，这能让巨型标题看起来更加干练大气。


---

## 📄 文档节点：all_pages_ui_wireframes.md <a id="all_pages_ui_wireframesmd"></a>

# 全站页面 UI 线稿总览（V1）

> 目标：先把所有页面的低保真 UI 骨架统一，保证布局、视觉语言、交互入口一致。

---

## 0) 全站统一规范

- **布局基线**：
  - 首页：Bento Box（4 宫格 + 大 Hero）
  - 专题页：Two-Column（左动态导航 + 右内容流）
  - Open Lab：双区（左侧控制/状态 + 右侧内容画布）
- **全局按钮层级**：Primary（实心）/ Ghost（描边）/ Tip（☕️打赏）
- **全局信息密度**：卡片首屏必须能看到“值不值得点”
- **全局打赏入口**：内容卡片、详情页底部、订阅页均可触达

---

## 1) 首页 `/`

### 1.1 Hero（数字名片 + 价值观）

```text
“通过创造实现自由，通过分享放大价值”

追求：创造 / 自由 / 分享
方法论：行动至上 / 尝试10次法 / 长期主义
竞争模式：专注 / 极致 / 口碑 / 快

🔥 X 个痛点在案 | 🤖 X 篇 AI 实验 | 📊 X 篇商业沉淀
[参与公开实验] [订阅复盘邮件]
```

### 1.2 四大能力引擎（Bento Grid）
- Open Lab（高对比、强行动）
- AI Experiments（终端风）
- Business Analysis（研报风）
- My Story（生活图文风）

### 1.3 Recent Drops
- 最近 4~6 条更新，按时间排序
- 每条含：日期 + 类型 + 标题

### 1.4 Footer 收口
- 邮箱订阅输入
- 社媒入口

---

## 2) 公开实验室 `/lab`

### 2.1 页面结构

```text
左侧：
- 提交灵感 CTA
- 状态过滤（全部/待确认/讨论中/研发中）
- 我的参与数据（可选）

右侧：
- 顶部排序（最新/最热）
- Hero Idea（在研项目大卡）
- Demand Pool（待确认与讨论中的卡片网格）
```

### 2.2 卡片层级
- **Idea 卡**：标题、状态、票数、摘要、进入详情
- **Feature 子建议**（详情页内）：可点赞、可回复

### 2.3 未立项 Idea 展示
- 左侧显示 Builder 初步评估/立项条件
- 右侧继续收集功能建议
- 顶部主按钮文案变为“催更”

---

## 3) AI 创造与实验 `/ai-experiments`（或 `/products`）

### 3.1 双栏结构（动态分类）

```text
左：动态分类导航（后台驱动）
右：内容流（按分类分段）
```

### 3.2 卡片信息
- 类型标签（Video/Article/Repo）
- 标题
- **Takeaways（1~3条）**
- 阅读/播放按钮
- ☕️ 打赏按钮

---

## 4) 商业分析 `/analysis`

### 4.1 双栏结构（同 AI 专题）
- 左：动态分类（宏观/公司研究/财报解析/商业思考）
- 右：研报式卡片流

### 4.2 卡片信息（更严谨）
- 日期、阅读时长、Ticker 标签
- 标题
- **Key Argument / Analysis Framework**
- 阅读全文按钮
- ☕️ 赞赏此分析

> 说明：该专题不导流 Open Lab，保持纯观点阅读体验。

---

## 5) 我的故事 `/my-story`

### 5.1 双栏结构（同 AI/商业）
- 左：动态分类（随笔/家庭生活/Vlog/学习成长）
- 右：生活志内容流

### 5.2 卡片信息（更有温度）
- 大封面图（优先）
- 标题
- Snippet（碎碎念）
- 阅读/观看按钮
- ☕️ 赞赏此文

---

## 6) 订阅与连接 `/subscribe`

### 6.1 三合一卡片
1. Newsletter（核心）
2. 加微信（二维码）
3. 打赏（微信/支付宝）

### 6.2 登录态分支
- 未登录：邮箱 + OTP 验证后登录并订阅
- 已登录：直接显示邮箱与订阅开关（无需重复输入）

---

## 7) 文章详情 `/post/[id]`

- 标题 + Meta + 外链主 CTA
- 正文区域
- 底部打赏组件（跳 `/subscribe` 或弹窗二维码）
- 返回入口

---

## 8) 后台与管理（管理员视角）

- Content 发布台（按 category 显示条件字段）
- 评论审核中心（pending/approved/rejected）
- Open Lab 状态流转台（Idea 与 Feature）
- 票数与互动看板（可选）

---

## 9) 下一步（建议）

1. 先做 **高保真视觉稿**（Figma）
2. 再落地 **前端页面壳**（静态数据）
3. 最后接入 **Payload 数据与 API**（动态数据）



---


# ==================================================
# 🗂️ 模块：主页 (Home)
# ==================================================

## 📄 文档节点：home_product_specs_v2.md <a id="home_product_specs_v2md"></a>

# 首页 (Home) 产品需求与交互说明 (v2)

## 1. 页面定位与核心心智
首页是 JESS.PU 个人站点的总枢纽。它的核心目标是：**“在 5 秒内讲清楚我是谁、我在做什么，并将流量精准分发到四大核心板块。”**
整体设计追求“大开大合”的极客风格，不搞小家子气的密密麻麻的信息流，而是用巨型色块、粗体大字和数据说话，将个人的多维能力（AI、商业、生活、共创）像画廊一样呈现出来。

---

## 2. 核心模块组成

### 2.1 巨型数字名片区 (Hero Section)
摒弃传统的“头像+简介”模式，采用更具视觉冲击力的**数字仪表盘 (Dashboard) 风格**。
*   **文案表达**：
    *   主标题 (H1)：`JESS.PU` (超大字号，占据视觉中心)
    *   副标题：`Builder / Thinker / Father` (或者 `AI 创造者 × 商业观察者`)
    *   Slogan：`“把成长做成一场公开实验。用创造获得自由，用分享放大价值。”`
*   **动态成就墙 (Live Stats)**：展示站点的“生命力”（通过 Payload CMS 聚合查询）：
    *   `🔥 X 个痛点在案` (读取 Idea 集合数)
    *   `🤖 X 篇 AI 实验` (读取对应 category 内容数)
    *   `📊 X 篇研报沉淀`
*   **核心行动点 (CTAs)**：
    *   `参与公开实验` (Primary，黑色实心/高亮背景)：直接锚点到下方的 Lab 区域或跳转 `/lab`。
    *   `订阅我的复盘` (Ghost)：跳转 `/subscribe`。

### 2.2 四大能力引擎入口 (The Core Pillars)
这是首页的骨架。将站内的四大核心专题，以**极具张力的大卡片 (Bento Box 风格)** 横向或网格状铺开，每个模块不仅是入口，还要透出最新的一条干货。

**板块 1：公开实验室 (Open Lab)**
*   **视觉**：跳跃的荧光色边框或背景。
*   **内容**：展示当前热度最高的 1 个“正在孵化”的 Idea。
*   **文案**：“你在想什么痛点？来决定我下一行代码写什么。”
*   **入口**：`[ 进入实验田 ↗ ]`

**板块 2：AI 创造经验 (AI Experiences)**
*   **视觉**：极简大气的纯净白底，通过考究的网格排版和留白突出干练的技术感与高信噪比。
*   **内容**：拉取该专题下最新的一条“Takeaways（核心结论）”。
*   **文案**：“Vibe Coding 实战、工作流自动化与最新模型硬核测评。”
*   **入口**：`[ 偷看我的 AI 工具箱 ↗ ]`

**板块 3：商业与宏观思考 (Business Analysis)**
*   **视觉**：严谨的华尔街日报/研报风格（白底，衬线字体）。
*   **内容**：拉取该专题下最新的一条“Key Argument（核心论点）”或股票代码。
*   **文案**：“用结构化框架看世界，剥离噪音，沉淀商业基本面理解。”
*   **入口**：`[ 阅读分析沉淀 ↗ ]`

**板块 4：我的故事与生活 (My Story)**
*   **视觉**：温暖的生活志风格，大面积使用该模块下最新的 Vlog 或文章封面图作为卡片背景。
*   **内容**：展示一句带有情感的“碎碎念 (Snippet)”。
*   **文案**：“代码和商业之外，一个普通人的成长、家庭与数字花园。”
*   **入口**：`[ 翻阅数字花园 ↗ ]`

### 2.3 最新更新信息流 (Recent Drops)
在展示完宏大的“能力骨架”后，补充一个时间线维度的信息流，证明站点是“活的”。
*   **数据来源**：调用 Payload CMS `Contents` 集合，跨 category 拉取状态为 `published` 的最新 6 条内容。
*   **卡片设计**：紧凑的列表，展示内容分类标签 (Tag，如 `[分析]`, `[Vlog]`)、标题和发布时间。
*   **跳转逻辑**：若配置了 `externalLink`，新标签页跳转原平台；若无，进入站内详情页 `/post/[id]`。

### 2.4 全局收口：订阅与联系 (Footer CTA)
*   **巨型色块引导**：页面最下方用一个与背景反差极大的大色块。
*   **文案**：“不想错过下一次的实验复盘和闭门分享？”
*   **组件**：直接在此处嵌入一个极简的【输入邮箱 -> 订阅】的 Input 组件，不强求用户跳转到 `/subscribe` 页面。旁边附上微信/小红书等社媒 Icon。

---


---

## 📄 文档节点：home_ui_wireframe.md <a id="home_ui_wireframemd"></a>

# 首页 (Home) UI/UX 交互线稿与视觉规划 (Bento Box 风格)

## 1. 整体视觉风格 (Visual Tone)
*   **风格基调**：**Bento Box (便当盒) 风格**。把各种不同属性的信息块（数据、图文、代码片段）装进圆角的卡片盒子里，形成网格化的大胆排版。
*   **空间感**：卡片之间留有明显的间隙，页面不再是自上而下一大坨字，而是高度模块化的“控制台”。

---

## 2. 页面结构线稿 (Wireframe)

### 2.1 巨型数字名片区与核心价值观 (Hero Dashboard)
将传统的 Hero 区设计为一份“结构化人生宣言”，把“追求”与“方法论”像脑图一样解构在首页最显眼的位置。

```text
[ 导航栏 Navbar ] ----------------------------------------------------

======================================================================
[ 黑色巨型模块块：占满全屏宽度的 100% ]

  JESS.PU
  
  “通过创造实现自由，通过分享放大价值”
  (超大字号 Slogan，作为视觉核心)

  --------------------------------------------------
  [ 核心价值观与方法论 (树状或网格解构) ]
  
  🎯 追求：
  ├─ 创造：做喜欢的事情
  ├─ 自由：追求财富自由与精神自由
  └─ 分享：秉承公开与互助的原则

  ⚙️ 方法论：
  ├─ 行动至上：Action speaks louder.
  ├─ 尝试10次法：不要被一次失败吓退。
  └─ 长期主义：做时间的朋友。
  
  ⚔️ 竞争模式 (小米模式)：
  └─ 专注、极致、口碑、快。

  --------------------------------------------------
  [ 动态成就数据条 ]
  🔥 15 个痛点在案  |  🤖 42 篇 AI 实验  |  📊 18 篇商业沉淀

  [ 参与公开实验 ↗ ]  [ 订阅复盘邮件 ]
======================================================================
```

### 2.2 四大能力引擎入口 (The Core Pillars - Bento Grid)
这里的排版是经典的“两行两列”或“拼图块”网格布局。每个盒子的视觉风格要与其对应的内容极度匹配。

```text
======================================================================
[ Bento Grid 容器 ]

+-----------------------------------+-----------------------------------+
| [ 盒子 1：公开实验室 ] (荧光绿底) | [ 盒子 2：商业与宏观 ] (白底黑线) |
|                                   |                                   |
| WE ARE BUILDING IN PUBLIC.        | 严肃的商业研报。                  |
|                                   |                                   |
| 📢 正在孵化最高票：               | 📊 最新观点：                     |
| 独立开发者订阅收入监控工具        | 腾讯游戏见顶后的新引擎在哪里？    |
| [状态：🚧 研发中 (128票)]         | [ 标签：$TCEHY | 财报解析 ]       |
|                                   |                                   |
| [ 进入实验田 ↗ ]                  | [ 阅读分析沉淀 ↗ ]                |
+-----------------------------------+-----------------------------------+
| [ 盒子 3：我的故事 ] (大图背景)   | [ 盒子 4：AI 实验 ] (深色终端)    |
|                                   |                                   |
| [背景图：一张有质感的家庭合照]    | root@jess:~# Vibe Coding 实战     |
|                                   |                                   |
| ✍️ 最新碎碎念：                   | 👉 最新干货提取：                 |
| 周末去山里放空了两天，断网的感    | 给 AI 坏例子比给好例子更有约束力。|
| 觉比任何 App 都管用...            | [ 标签：Prompt 技巧 ]             |
|                                   |                                   |
| [ 翻阅数字花园 ↗ ]                | [ 偷看 AI 工具箱 ↗ ]              |
+-----------------------------------+-----------------------------------+
======================================================================
```

### 2.3 最新动态信息流 (Recent Drops)

```text
[ 模块标题：最新动态 (RECENT DROPS) ] -----------------------------------

* [ 📅 02-28 ] [ 🏷️ Vlog ]     周末去山里放空的两天 (My Story)
* [ 📅 02-27 ] [ 🏷️ Article ]  如何用 Cursor 3天跑通一套 MVP (AI 实验)
* [ 📅 02-25 ] [ 🏷️ Analysis ] 2026 降息周期下的资产配置逻辑 (商业分析)
* [ 📅 02-20 ] [ 🏷️ Update ]   Open Lab 上线啦！(站点公告)

[ 查看所有归档 ↗ ]
```

### 2.4 全局收口 (Footer CTA)

```text
======================================================================
[ 巨型强调色块 (比如亮黄色背景) ]

  不想错过下一次的实验复盘和闭门分享？
  
  [ 输入你的 Email 地址...          ]  [ 立即订阅 ]
  
  [WeChat Icon]  [RedBook Icon]  [GitHub Icon]  [X/Twitter Icon]
======================================================================
```


---


# ==================================================
# 🗂️ 模块：开放实验室 (Open Lab)
# ==================================================

## 📄 文档节点：open_lab_product_specs.md <a id="open_lab_product_specsmd"></a>

# 公开实验室 (Open Lab) 产品需求与业务路径说明

## 1. 产品定位与核心理念
**“公开实验室 (Open Lab)”** 是 JESS.PU 网站的核心互动模块定。它的灵感来源于“Build in Public（公开构建）”，旨在建立一个完全透明、由用户需求直接驱动的功能实验场。
在这里，任何用户都可以提出需求、发现痛点，并通过点赞投票决定开发优先级。站长（管理员）将在这个全公开的看板上对需求进行立项、研发、复盘与归档，实现紧密的用户共创生态。

---

## 2. 用户角色定义
*   **访客/普通用户 (User)**：浏览实验室看板，提出新需求，对感兴趣的需求点赞投票，参与需求底下的评论区讨论（目前支持匿名或简易认证）。
*   **站长/管理员 (Admin)**：审核用户提交的新需求，更新需求所处的生命周期状态（Status），审核用户评论，主导产品迭代方向。

---

## 3. 核心功能结构

### 3.1 前端广场 (Frontend - `/lab` 页面)
*   **大盘数据展示**：顶部包含总连接数（已立项需求等）、处理中、已完成等核心数据指标统计。
*   **动态分段排序控件**：用户可点击切换 **“最新动态 (Latest)”** 或 **“最高热度 (Top Voted)”**，以不同维度探索灵感池。
*   **看板分类流**：
    *   **待定灵感池 (Demand Pool)**：展示状态为 `pending` (待澄清) 和 `discussing` (讨论中) 的卡片。
    *   **实验行进区 (Experiment Progress)**：展示状态为 `approved` (已立项), `in-progress` (实验中), `reviewed` (已复盘) 具有高优先级的卡片。
*   **卡片交互**：
    *   **状态与优先级标签**：直观展示当前卡片所处的研发阶段，以及系统计算出的“优先级评分”。
    *   **“我也想要”投票微交互**：用户点击点赞按钮后触发弹跳特效 (`Pop`)。
    *   **评论与讨论**：用户可展开需求卡片，发表实名/匿名评论以补充场景。
    *   **快速提交入口**：悬浮的“提交灵感”表单，支持快捷输入标题、痛点和适用人群。

### 3.2 后端与管理台 (Admin - Payload CMS 仪表盘)
*   **需求管理 (`Needs` 集合)**：管理员在此审阅前端提交的表单。核心操作包括：修改状态栏（如将 `pending` 提升为 `approved`），补充内部预估的 `impactScore`, `effortScore`, `reusabilityScore`，系统将据此与用户票数一起动态重新计算最终的 `priorityScore`（优先级评分）。
*   **防刷票审计 (`Votes` 集合)**：记录每一侧点赞行为与 `deviceHash`（或关联具体 User），保障“一人一票”机制。
*   **评论管控 (`Comments` 集合)**：默认用户评论处于隐藏状态，需管理员在后台勾选 `approved`（审核通过）后，方可展现在前端页面。

---

## 4. 用户体验路径 (User Journeys)

### 4.1 路径 A：用户提出新需求 (Submit a Need)
1. **触发**：用户在 `/lab` 页面浏览时，未找到解决自己痛点的方案，点击“提交新灵感”。
2. **表单填写**：填写“需求标题”、“想解决的问题”等信息提交。
3. **入库等待**：表单通过 `/api/submit-need` 路由调用后端，在 Payload `Needs` 集合中创建一条新记录。此时状态默认为 `pending`（待澄清）。
4. **前端反馈**：前端提示提交成功。

### 4.2 路径 B：用户参与需求投票 (Upvote a Need)
1. **浏览与发现**：用户浏览需求卡片，或者通过“最高热度”排序发现感兴趣的提案。
2. **产生共鸣并点击**：用户点击 **“我也想要”** 按钮。
3. **后端校验与计算**：
    * 调用 `/api/needs/[id]/vote/route.ts` 接口。
    * 后端根据 IP Header 获取当前 `deviceHash`，检查 `Votes` 集合中是否已存在该条 (Need, IP) 记录。
    * 若已存在，返回错误“已投过票”；若不存在，新建一条 `Votes` 记录，并将该 Need 的总票数加一。
    * 重新计算并保存该 Need 的综合 `priorityScore`。
4. **前端动效**：按钮触发缩放变色特效 (`vote-pop` CSS 动画)，数字实时 `+1` 显示。

### 4.3 路径 C：用户在需求中参与讨论 (Discussing a Need)
1. **浏览与发现**：用户在浏览需求卡片时，发现感兴趣或与自己痛点高度契合的提案，点击卡片展开详情或直接进入评论区。
2. **发表观点与补充场景**：
    * 用户可以在评论区留言，补充自己遇到该痛点的具体场景、提供解决方案建议，或者对管理员的追问进行回复。
    * 评论支持轻量级 Markdown 格式，以便更清晰地表达想法。
3. **身份状态**：
    * 未登录用户可匿名或使用简易昵称发表评论。
    * 已登录用户（若接入账号体系）则展示用户头像与身份标识。
4. **审核与展示**：评论提交后，默认状态为“待审核（隐藏）”，待管理员在后端审核通过（勾选 `approved`）后，实时在前端评论流中展示，形成健康的讨论生态。

### 4.4 路径 D：管理端需求全生命周期流转与研发过程公开 (Lifecycle & Build in Public)
作为“公开实验室”的核心特色，管理员（开发者）不仅是状态的变更者，更是开发过程的展示者。
1. **清理需求池与立项评估**：
    * 管理员定期查看 `pending` 需求。
    * 评估并填写 `影响面 (Impact)` 和 `开发成本 (Effort)` 等因子。若需求不清晰，管理员将状态改为 `discussing` 并以带有“开发者 (Admin)”专属徽章的身份在评论区发起追问；若决定做，则改为 `approved`（已立项）。
2. **微更日志 (Micro-Changelog) 与过程展示**：
    * 当状态流转为 `in-progress`（实验中）时，系统支持管理员在该需求下发布“进度快照 (Updates)”。
    * **展示内容**：管理员可上传 UI 设计草图、核心代码片段截图、或者遇到了什么技术坑等“开发内幕”。
    * **用户感知**：关注该需求的用户能像追剧一样，看到需求从“文字”一点点变成“代码”和“界面”，极大增强参与感。
3. **研发进度与版本号联动**：
    * 管理员可在后台为进行中的需求关联“预计上线版本号 (Target Version)”（如 v1.2.0）。前台卡片会同步显示“预计于 v1.2.0 上线”，给用户明确的心理预期。
4. **完结交付与成果宣发**：
    * 功能上线后，管理员将状态置为 `reviewed`（已复盘）。
    * **交付物展示**：需求卡片内会补充“最终成果链接”、“操作演示视频/GIF”或“复盘文章链接”。
    * 需求生命周期闭环收尾，该卡片成为产品进化史上的一个永久里程碑。

### 4.5 路径 E：用户反馈与闭环 (Feedback Loop)
1. **状态变更通知**：
    * 当某个需求的状态发生关键流转（例如从 `pending` 变为 `approved`，或者从 `in-progress` 变为 `reviewed` 交付上线）时，系统会触发通知机制。
2. **邮件触达**：
    * 针对已登录用户或在提交需求/评论时留下了邮箱的用户，系统通过服务（如 Resend 或 SendGrid）发送自动化邮件。
    * 邮件内容包含：状态变更告知、管理员的复盘链接或感谢语。
3. **闭环致谢**：
    * 当需求最终完成（`reviewed`）并转化为网站的正式文章或功能时，文章末尾会特别致谢（@提及）提出该需求以及在评论区贡献高赞观点的用户，增强社区的“共创荣誉感”。

---

## 5. 底层数据结构 (Data Architecture)

围绕公开实验室，Payload CMS 构建了四位一体的数据关联：

*   **`Users` (用户表)**：支撑未来会员登录及贡献者识别（包含头像、简介、角色等扩展属性）。
*   **`Needs` (需求大表)**：系统的中枢核心。包含业务相关的元信息（痛点描述、投票数、状态机枚举、各种计算得分因子等）。
*   **`Votes` (投票审计表)**：关联 `users` (或 IP) 与 `needs`。防止重复计算，并为未来做用户侧的“我点赞过的榜单”提供底层数据。
*   **`Comments` (讨论回复表)**：关联 `needs` 与 `users`。提供审核字段 `approved: Boolean` 以支持文本安全。


---

## 📄 文档节点：open_lab_ui_wireframe.md <a id="open_lab_ui_wireframemd"></a>

# 公开实验室 (Open Lab) UI/UX 交互线稿与视觉规划 (基于 Idea -> Feature 架构)

## 1. 整体视觉风格 (Visual Tone)
*   **风格基调**：**Brutalism (粗野主义) 混搭 Geek 极简风**。大字号、粗线条、高对比度的黑白灰主色，配合一个极其跳跃的“品牌色”（比如荧光绿/电光蓝）作为唯一的视觉焦点（如点赞按钮、状态标签）。
*   **排版逻辑**：打破常规的对称网格，采用“卡片瀑布流”的排版。外层是“大灵感(Ideas)”，内层是“具体功能(Features)与进度”。

---

## 2. 页面结构线稿 (Wireframe)

### 2.1 顶级入口广场 (The Idea Board)

```text
[ 导航栏 Navbar ] ----------------------------------------------------

[ 巨型粗体标题 ]
WE ARE BUILDING IN PUBLIC.
[ 小字描述 ] 在这里，每一个痛点都可能变成下一个产品。提出你的想法，决定我的代码。

[ 数据大盘面板 (粗边框卡片) ]
  💡 52 个创意在案   |   ⚙️ 3 个产品正在爆肝   |   🚀 12 个工具已交付

[ 悬浮的大号主操作按钮 ] 
+ 提交一个新产品灵感 (点击弹出全屏抽屉)

----------------------------------------------------------------------
[ 排序与过滤组件 ]
| 👉 最新灵感 (Latest) | 🏆 最高热度 (Top Voted) | 🔄 正在研发 (In Progress) |
```

### 2.2 灵感卡片流 (Idea Card Stream) - 以“最高热度”为例

```text
======================================================================
[ Idea 卡片 1 ] (处于 In-Progress 状态，高亮展示大方向)
----------------------------------------------------------------------
[状态：🚧 研发中 (v1.0 即将测试)]           [热度：🔥 128]

[ 巨型标题 ] 独立开发者订阅收入大盘监控工具
[ 描述 ] “现在查几个不同平台的订阅收入得来回切，希望能有一个聚合面板一目了然。”

-- 进度速览 --
[Builder] 刚刚更新：Stripe 接口调通了！(附截图...)

👉 [ 展开查看进度日志 & 参与 15 个具体功能的讨论 ]
======================================================================

======================================================================
[ Idea 卡片 2 ] (处于 Pending 状态，等待票数)
----------------------------------------------------------------------
[状态：💡 等待共鸣]                         [热度：🔥 45]

[ 巨型标题 ] 微信读书笔记一键转 Notion 插件
[ 描述 ] “现在导出太折腾了，想要个优雅的自动化方案。”

👉 [ 展开详情：投票支持 & 提出你的功能期望 ]
======================================================================
```

### 2.3 灵感详情页 (Idea Detail / Modal) - 核心双轨互动区
当用户点击某一个 Idea 后进入的详情页面。采用**左右分栏**（PC端）或**上下分栏**（移动端）。

**情景 A：已立项/研发中的 Idea (如：订阅监控工具)**
```text
[ 返回看板 ]   [ 分享此灵感 ]

================== 顶部：核心概念区 ==================
[ 独立开发者订阅收入大盘监控工具 ]
提出者：@Alex | 状态：🚧 研发中
[ 👍 我也需要这个产品 (128票) ] (超大按钮，点击弹跳特效)

----------------------------------------------------
[ 左栏/主栏：Builder 的造物日志 (Changelog / Timeline) ]

👩‍💻 @JESS.PU (Builder)：
> 2月28日：产品初步 UI 设计稿出炉。
> [大图展示]

👩‍💻 @JESS.PU (Builder)：
> 2月27日：立项！决定先跑通 Stripe 和 支付宝的接口。

(像博客的时间线，按时间倒序排布，展现产品的生命演进)

----------------------------------------------------
[ 右栏/侧边栏：子需求/功能共创区 (Feature Requests) ]

[ 标题：你希望这个产品包含什么功能？ ]
[ 输入框：+ 提议一个具体功能... ]

> 功能提议 1：希望能支持微信支付的流水接入 (👍 12票)
  └ @Builder 回复：这个做进 v1.1 里，微信接口正在申请。
----------------------------------------------------
```

**情景 B：未立项/等待共鸣的 Idea (如：微信读书插件)**
```text
[ 返回看板 ]   [ 分享此灵感 ]

================== 顶部：核心概念区 ==================
[ 微信读书笔记一键转 Notion 插件 ]
提出者：@某网友 | 状态：💡 等待共鸣
[ 👍 猛击这里催更 (45票) ] (此时文案偏向“催更/投票”)

----------------------------------------------------
[ 左栏/主栏：Builder 的初步评估 (Initial Thoughts) ]

(此时尚未立项，这里展示的是“立项条件”或“Builder 评估”)

👩‍💻 @JESS.PU (Builder)：
> 【初步评估】这个痛点很痛！不过目前还需要明确一下：大家是更想要一个纯本地的桌面端，还是一个浏览器插件？请在右侧留言，满 100 票我立刻开始画原型！

(如果连评估都没有，则展示幽默的占位图：比如一张咖啡杯底朝天的图，配字：“Builder 还在观察这个灵感的潜力，快用投票砸醒他！”)

----------------------------------------------------
[ 右栏/侧边栏：子需求/功能共创区 (Feature Requests) ]

[ 标题：如果这个做出来，你最想要什么功能？ ]
[ 输入框：+ 提议一个具体功能... ]

> 功能提议 1：必须是浏览器插件才方便！(👍 20票)
> 功能提议 2：希望能自动带上原文的高亮样式 (👍 15票)
----------------------------------------------------
```
*   **交互亮点**：
    *   **双轨并行**：左边是 Builder 的主场（秀进度、秀肌肉、讲故事）；右边是用户的客场（提功能、投票、激烈讨论）。
    *   **颗粒度清晰**：用户一开始在外部只对“大概念”投票，进来以后才会对“细枝末节（比如有没有深色模式、接不接特定支付）”进行提议。这就解决了你说的“Idea vs 软件具体功能”的矛盾。

---

## 3. 核心设计原则总结 (Design Principles)
1. **层次分明**：看板层只展示“宏大叙事（Idea）”，详情层才展示“具体功能（Features）”。
2. **Builder 的舞台**：左侧/主干的进度流是视觉核心，强化“有人正在为你写代码”的真实感。
3. **微共创体验**：把右侧的功能区做成类似 GitHub Issues 列表的感觉，但是更轻量、更易点赞（Reddit 的 Upvote 体验）。


---


# ==================================================
# 🗂️ 模块：AI实验 (AI Experiments)
# ==================================================

## 📄 文档节点：ai_experiments_product_specs.md <a id="ai_experiments_product_specsmd"></a>

# AI 创造与实验专题 (AI Experiments) 产品需求与交互说明

## 1. 页面定位与核心理念
将过去单纯的“产品展示”拓展为更立体的**“AI 创造实录与前沿探索”**。
这里不仅有从 Open Lab 里孵化出来的产品复盘，还包括了站长在“公开实验”过程中的实践经验、Vibe Coding 技巧、工作流分享，以及对最新 AI 模型的硬核测评。
**核心心智**：“看一个 Builder 是如何用 AI 武装到牙齿的”。通过干货分享，建立专业信任，最终将流量导向个人的私域与公开实验室。

---

## 2. 用户角色与目标
*   **探索者/小白**：想了解现在 AI 能做到什么程度，寻找最新的模型测评和基础入门方法。
*   **创造者/同行 (Builders)**：想看 Vibe Coding 的真实踩坑记录，学习具体的工作流、Prompt 技巧和代码架构。
*   **站长 (Admin)**：将自己的实践过程体系化沉淀，通过“Takeaways（核心结论）”快速交付价值，并向其他板块（如 Lab）引流。

---

## 3. 核心功能结构与布局 (`two-column` 布局)

页面采用**经典的左右双栏**（左侧导航，右侧内容流），去除了繁杂的第三栏，让用户的视觉完全聚焦于干货本身。

### 3.1 左侧：动态导航侧栏 (Dynamic Side Nav)
*   **非硬编码设计**：摒弃写死的分类。分类导航树（Categories）直接通过读取 Payload CMS 中该专题下的现有分类动态生成。
*   **平滑滚动**：点击左侧某个分类（如“工作流与技巧”），右侧主体内容平滑滚动至对应锚点。
*   **实验室导流 (CTA)**：在导航树的最下方，固定挂载一个醒目的入口：“没找到想要的？👉 去 Open Lab 提需求”，实现“阅读 -> 共创”的闭环。

### 3.2 右侧：内容瀑布流 (Main Content Stream)
*   后端根据动态分类遍历拉取内容，形成错落有致的卡片瀑布流。
*   **高信息密度卡片设计**：
    *   **Meta 信息**：类型标签（`[Video]`, `[Article]`, `[Repo]`）、发布日期。
    *   **主标题**：清晰的动作导向标题（如“如何用 Cursor 3天做完 xxx”）。
    *   **🔥 Takeaways (核心结论前置)**：带浅色背景的区块，提炼 1-3 条核心干货。用户无需点击进入详情，即可获得阅读价值。
    *   **双排跳转**：根据后台配置，提供 [ 播放视频 ↗ ] 或 [ 阅读文字版 ↗ ] 的分流按钮。

---

## 4. 用户体验路径 (User Journeys)

### 4.1 路径 A：高效获取干货 (Quick Consume)
1. **进入页面**：用户进入 `/ai-experiments` 专题。
2. **扫描导航**：在左侧导航栏看到自己感兴趣的领域（比如“工作流与技巧”），点击直达。
3. **阅读结论**：在卡片列表中，用户扫读带背景色的“Takeaways”区块，迅速获得核心认知（比如“给 AI 坏例子比给好例子更管用”）。
4. **决定深度**：如果结论击中痛点，用户点击卡片进入 `/post/[id]` 查看完整复盘文章或跳转去 B站 看演示视频。

### 4.2 路径 B：从看客转变为共创者 (Reader to Co-creator)
1. **建立信任**：用户阅读了多篇硬核的实战复盘，认可了站长的创造能力和技术栈。
2. **产生共鸣**：用户在看“工具箱”或文章时，联想到自己一直没被满足的一个痛点。
3. **导流转化**：用户看到右侧栏的提示“遇到没解决的痛点？去 Open Lab 提需求”，点击进入 `/lab` 页面。
4. **闭环**：用户在 Lab 提交了一个新 Idea，完成了从单向“吸收内容”到双向“参与共创”的身份跃迁。

---

## 5. 数据结构与底层支撑
*   复用现有的 Payload CMS `Contents` 集合。
*   **需增加的字段扩展**：
    *   在 `Contents` 集合中，针对此专题的内容增加一个 `takeaways` 字段（Array of Strings 或富文本），专供列表卡片前置渲染核心结论使用。
    *   丰富 `category` 的枚举值，以支撑左侧导航的精确过滤（如 `build-in-public`, `workflow`, `ai-frontier`）。


---

## 📄 文档节点：ai_experiments_ui_wireframe.md <a id="ai_experiments_ui_wireframemd"></a>

# AI 创造与实验专题 (AI Experiments) UI/UX 交互线稿与视觉规划

## 1. 整体视觉风格 (Visual Tone)
*   **风格基调**：与 Open Lab 保持一致的 **Geek 极简风**，但比 Lab 的“生猛”稍微收敛一点，更偏向“杂志/画册”的阅读体验。
*   **排版逻辑**：左（导航）- 中（内容瀑布）- 右（工具箱/导流）的 Multi-shell 结构，滚动时左右两边吸顶不动。

---

## 2. 页面结构线稿 (Wireframe)

### 2.1 顶部横幅 (Hero Section)

```text
[ 巨型粗体标题 ] AI 使用经验
[ 副标题 ] 这里没有水文，只有从“公开实验”里蹚出来的真金白银。涵盖产品复盘、Vibe Coding 技巧与前沿模型测评。
```

### 2.2 核心双栏结构 (Two-Column Layout)
抛弃繁杂的三栏，采用更聚焦的 **“左侧导航字典 + 右侧干货瀑布流”** 结构。
左侧导航为动态读取，不写死分类，随着内容的丰富可随时在后台新增大类。

```text
[ 左侧栏 (Dynamic Nav) - 吸顶 ]     [ 右侧主内容区 (Main Content Stream) ]
                                |
📍 分类导航 (后台动态配置)         |   ==== 🛠️ 实战复盘 (Build in Public) ====
                                |   [卡片] 视频：我是如何用 Cursor 3天做完 xxx 的？
- 🛠️ 实战复盘                     |   [卡片] 文章：Open Lab 需求孵化器 MVP 落地全复盘
- ⚡ 工作流与技巧                  |   [卡片] 文章：独立开发者如何做支付接入？
- 🧠 模型测评与前沿                |
- [未来可能新增的分类...]           |   ==== ⚡ 工作流与技巧 (Workflows) ====
                                |   [卡片] 仓库：一套拿来即用的 Next.js 极简模板
👇 没找到想要的？                 |   [卡片] 视频：Vibe Coding 进阶：如何写出让 AI 听话的 Prompt
[ 去 Open Lab 提需求 🚀 ]        |   [卡片] 文章：如何用 AI 自动化我的 RSS 信息流
                                |
                                |   ==== 🧠 模型测评与前沿 (Reviews & Frontier) ====
                                |   [卡片] 文章：Gemini 1.5 Pro 深度测评：它真的能取代 Claude 吗？
                                |   [卡片] 视频：2026 个人开发者还值得做套壳站吗？
```

### 2.3 内容卡片细节 (Card Detail)
为了提升专业感，卡片需要展示“核心干货”，让用户不用点进去就知道值不值得看。

```text
------------------------------------------------------------
[ 🏷️ Video | 📅 2026-02-28 ] 
大字标题：Vibe Coding 进阶：如何写出让 AI 听话的 Prompt

[摘要/Takeaways 模块 - 带背景色]
👉 核心结论：
1. 不要教 AI 怎么写代码，告诉它你的系统架构。
2. 给出“坏例子”比给出“好例子”更有约束力。
3. 附带了一套我的 .cursorrules 模板。

[ 播放视频 ↗ ]   [ 阅读文字版 ↗ ]   |   [ ☕️ 觉得有用？请我喝咖啡 ]
------------------------------------------------------------
```


---


# ==================================================
# 🗂️ 模块：商业分析 (Business Analysis)
# ==================================================

## 📄 文档节点：business_analysis_product_specs.md <a id="business_analysis_product_specsmd"></a>

# 商业分析专题 (Business Analysis) 产品需求与交互说明

## 1. 页面定位与核心理念
**商业分析专题 (`/analysis`)** 是 JESS.PU 沉淀对于宏观大势与微观商业理解的核心阵地。
*   **核心心智**：用结构化的框架看世界，减少信息噪音，聚焦于干货输出。
*   **内容范畴**：涵盖宏观经济趋势、行业赛道演进、微观商业公司（基本面、财报解析）的独立看法和深度观点。
*   **展示策略**：不再是零散的读后感，而是如同“投研报告”一般的模块化沉淀。

---

## 2. 页面结构与布局 (`two-column` 双栏布局)

与“AI 创造与实验专题”保持一致的阅读心智，采用更聚焦的**左侧动态导航 + 右侧卡片流**。

### 2.1 左侧：动态导航侧栏 (Dynamic Side Nav)
*   **动态分类树**：基于 Payload CMS 中该专题下的现有分类动态渲染。例如：
    *   🌍 宏观大势 (Macro & Trends)
    *   🏢 公司研究 (Company Fundamentals)
    *   💰 财报解析 (Earnings Breakdown)
    *   🤔 商业思考 (Thoughts & Opinions)
*   **锚点滚动**：点击分类名，右侧主体无缝滚动至对应区块。

### 2.2 右侧：研报式内容流 (Analysis Content Stream)
这里的卡片设计需要比 AI 专题更强调**“结构化分析视角”**。

*   **高信息密度卡片设计 (Analysis Card)**：
    *   **Meta 信息**：日期、阅读时长估算、核心研究对象标签（如 `$NVDA`, `$TCEHY`, `宏观经济`）。
    *   **主标题**：明确的分析主题（例如：“腾讯 2025Q4 财报拆解：游戏见顶后的新引擎在哪里？”）。
    *   **📊 核心观点/分析框架 (Key Lens)**：
        *   （取代 AI 专题的 Takeaways），这里突出你的**分析框架和核心论点**。
        *   例如：【核心论点】：视频号电商转化率提升 2% 将带来 xxx 增量。
        *   例如：【估值判断】：目前处于被低估的左侧，安全边际高。
    *   **操作按钮**：[ 深入阅读分析全文 ↗ ]

---

## 3. 用户体验路径 (User Journeys)

### 3.1 路径 A：机构式/研究式的纯粹阅读
1. **进入聚合页**：用户进入 `/analysis`，左侧清晰地展示了宏观、公司、财报等模块。
2. **扫描核心论点**：在右侧的卡片流中，用户直接通过浅色背景块中的【核心论点】或【估值判断】快速获取核心观点，过滤掉不需要的信息噪音。
3. **深度验证**：若用户对该论点感兴趣或持有不同意见，点击进入 `/post/[id]` 查看详细论证逻辑、数据引用和图表。
4. **沉淀信任**：通过连续阅读高质量的结构化输出，用户建立起对站长商业分析能力的信任，完成心智的建立（后续可转化为订阅或付费咨询等私域动作）。

---

## 4. 数据结构与底层支撑 (CMS Schema 建议)

*   **扩展 `Contents` 集合的元数据**：
    *   针对 `category: 'business-analysis'` 的内容，除了正文 `body` 外，建议在后台增加专用的结构化字段，例如：
        *   `keyArgument` (核心论点 - 字符串/简短文本，用于卡片外显)
        *   `analysisFramework` (分析框架标签，如：单位经济模型、竞争格局等)
        *   `ticker` (关联股票代码，可选，用于快速生成标签如 `$BABA`)


---

## 📄 文档节点：business_analysis_ui_wireframe.md <a id="business_analysis_ui_wireframemd"></a>

# 商业分析专题 (Business Analysis) UI/UX 交互线稿与视觉规划

## 1. 整体视觉风格 (Visual Tone)
*   **风格基调**：**金融/研报极简风 (Financial & Research Minimalist)**。相比于 AI 专题的“极客感”，商业分析应该给人一种“冷峻、严谨、结构化”的感觉。多使用衬线字体（Serif，如用于引言或论点），表格线框，以及类似于华尔街日报/彭博社的严肃排版。
*   **排版逻辑**：左侧动态导航（索引），右侧卡片瀑布流（研报摘要）。

---

## 2. 页面结构线稿 (Wireframe)

### 2.1 顶部横幅 (Hero Section)

```text
[ 巨型粗体标题 ] 商业分析与宏观思考
[ 副标题 ] 用结构化的框架看世界，剥离噪音，沉淀对宏观大势与微观公司（基本面/财报）的独立理解。
```

### 2.2 核心双栏结构 (Two-Column Layout)

```text
[ 左侧栏 (Dynamic Nav) - 吸顶 ]     [ 右侧主内容区 (Analysis Card Stream) ]
                                |
📍 研报分类 (后台动态配置)         |   ==== 🌍 宏观大势 (Macro & Trends) ====
                                |   [卡片] 2026 降息周期下的资产配置逻辑重构
- 🌍 宏观大势                     |   [卡片] 为什么说这波出海和以往完全不同？
- 🏢 公司研究                     |   
- 💰 财报解析                     |   ==== 💰 财报解析 (Earnings Breakdown) ====
- 🤔 商业思考                     |   [卡片] 腾讯 2025Q4 财报拆解：游戏见顶后的新引擎
                                |   
                                |   ==== 🏢 公司研究 (Company Fundamentals) ====
                                |   [卡片] 深度：一文看懂拼多多跨境业务的单位经济模型
                                |   [卡片] 苹果的“创新者窘境”是不是伪命题？
```

### 2.3 分析卡片细节 (Analysis Card Detail)
商业分析的卡片必须一眼让人看到“硬核的结论”或“股票代码”，而不是标题党。

```text
------------------------------------------------------------
[ 📅 2026-02-28 | ⏱️ 12 min read | 🏷️ $TCEHY ] 

大字严肃标题：腾讯 2025Q4 财报拆解：游戏见顶后的新引擎在哪里？

[ 📊 核心观点 / Key Argument - 带浅色网格背景 ]
💡 结论先行：
- 视频号电商转化率每提升 2%，将直接带来约 xxx 亿的净利润增量。
- 传统游戏业务虽触顶，但海外发行渠道的把控力被严重低估。
- 估值判断：左侧，具有较高安全边际。

[ 深入阅读分析全文 ↗ ]   |   [ ☕️ 赞赏此分析 ]
------------------------------------------------------------
```

```text
------------------------------------------------------------
[ 📅 2026-02-20 | ⏱️ 8 min read | 🏷️ 宏观经济 ] 

大字严肃标题：2026 降息周期下的资产配置逻辑重构

[ 📊 分析框架 / Analysis Framework - 带浅色网格背景 ]
🔎 本文使用框架：美林时钟变体 + 中美利差模型
- 重点探讨了在“宽货币+紧财政”的错配下，黄金与大宗商品的非对称反应。

[ 深入阅读分析全文 ↗ ] 
------------------------------------------------------------
```


---


# ==================================================
# 🗂️ 模块：我的故事 (My Story)
# ==================================================

## 📄 文档节点：my_story_product_specs.md <a id="my_story_product_specsmd"></a>

# 我的故事专题 (My Story) 产品需求与交互说明

## 1. 页面定位与核心理念
**我的故事专题 (`/my-story`)** 是 JESS.PU 展现“人味儿”和真实生活状态的板块。
*   **核心心智**：在硬核的 AI 代码和严肃的商业分析之外，展现一个活生生的、有血有肉的 Builder 的日常。
*   **内容范畴**：涵盖个人成长经历、家庭故事、日常 Vlog 记录，以及软技能的学习经验（如英语学习方法、精力管理等）。
*   **展示策略**：相较于商业分析的“冷峻”，这个板块需要更有温度、更偏向“个人数字花园 (Digital Garden)”或“生活志 (Lifestyle Journal)”的松弛感。

---

## 2. 页面结构与布局 (`two-column` 双栏布局)

沿用全站统一的双栏结构，降低开发维护成本，但在视觉呈现和卡片细节上做出“生活感”的差异化。

### 2.1 左侧：动态导航侧栏 (Dynamic Side Nav)
*   **动态分类树**：基于 Payload CMS 中该专题下的分类动态渲染。例如：
    *   📖 个人随笔 (Personal Essays)
    *   👨‍👩‍👧 家庭与生活 (Family & Life)
    *   📹 日常 Vlog (Vlogs)
    *   🧠 学习与成长 (Learning & Growth)
*   **锚点滚动**：点击分类名，右侧主体无缝滚动至对应区块。

### 2.2 右侧：生活志内容流 (Lifestyle Content Stream)
与商业分析的“严谨结论”不同，这里的卡片更强调**视觉表现力**和**情绪传递**。

*   **图文并茂的卡片设计 (Story Card)**：
    *   **大面积封面图 (Cover Image)**：如果是 Vlog 或生活记录，封面图是第一要素，最好带有拍立得或胶片边框质感。
    *   **Meta 信息**：日期（对于生活记录来说，时间戳非常重要，体现岁月感）、类型（文章/视频）。
    *   **主标题**：感性、随意的标题（如：“周末和家人去山里放空的两天”）。
    *   **摘录/碎碎念 (Snippet/Thoughts)**：
        *   取代商业分析的【核心论点】。
        *   这里展示一段摘录，或者一小段富有情感的“碎碎念”引言，字号可以稍微区别于正文，比如使用手写体风格（可选）。
    *   **操作按钮**：[ 阅读全文 ↗ ] 或 [ 观看 Vlog ↗ ]

---

## 3. 用户体验路径 (User Journeys)

### 3.1 路径 A：粉丝/订阅者的情感共鸣
1. **进入聚合页**：用户被硬核技术或商业分析吸引后，想了解站长是个什么样的人，点进 `/my-story`。
2. **浏览日常**：左侧看到家庭、随笔等生活化标签。右侧被大面积的照片和感性的碎碎念吸引。
3. **产生连接**：阅读了一篇关于英语学习的经历，或者看了一段家庭 Vlog，感受到了共鸣。
4. **促成关注**：这种真实感极大地拉近了心理距离，从而提高了用户去 `/subscribe` 页面留下邮箱或关注小红书的转化率。

---

## 4. 数据结构与底层支撑 (CMS Schema 建议)

*   **扩展 `Contents` 集合的元数据**：
    *   复用 `Contents`，但当 `category: 'my-story'` 时，前端展示逻辑分支。
    *   关键字段：
        *   `coverImage` (封面大图：生活志必需品)
        *   `snippet` (碎碎念/感性导语：用于卡片外显，替代硬核的 takeaways)


---

## 📄 文档节点：my_story_ui_wireframe.md <a id="my_story_ui_wireframemd"></a>

# 我的故事专题 (My Story) UI/UX 交互线稿与视觉规划

## 1. 整体视觉风格 (Visual Tone)
*   **风格基调**：**数字花园/生活志 (Digital Garden / Lifestyle)**。不同于 AI 的“极客风”和商业的“研报风”，这里需要**人味儿**和**松弛感**。可以增加更多的留白，照片可以加上轻微的拍立得/胶片边框效果。文字排版可以稍微随性一些，比如引言可以用带有点手写感的斜体字。
*   **排版逻辑**：沿用全站的左导航右内容双栏布局，但右侧的卡片变成了“大图+碎碎念”模式。

---

## 2. 页面结构线稿 (Wireframe)

### 2.1 顶部横幅 (Hero Section)

```text
[ 巨型粗体标题 ] 我的故事与生活
[ 副标题 ] 代码和商业之外的真实生活。记录成长、学习方法、家庭日常，以及一些毫无用处的发呆时刻。
```

### 2.2 核心双栏结构 (Two-Column Layout)

```text
[ 左侧栏 (Dynamic Nav) - 吸顶 ]     [ 右侧主内容区 (Lifestyle Card Stream) ]
                                |
📍 生活侧影 (后台动态配置)         |   ==== 👨‍👩‍👧 家庭与生活 (Family & Life) ====
                                |
- 📖 个人随笔                     |   [ 大画幅封面图：一张阳光下的小孩或家庭合照 ]
- 👨‍👩‍👧 家庭与生活                 |   [ 📅 2026-02-28 | 📹 Vlog ]
- 📹 日常 Vlog                    |   主标题：周末去山里放空的两天
- 🧠 学习与成长                   |   [✍️ 碎碎念]: "平时盯着屏幕太久了，周末决定断网48小时。大自然的白噪音比任何 App 都管用..."
                                |   [ 观看 Vlog ↗ ]
                                |   
                                |   ==== 🧠 学习与成长 (Learning & Growth) ====
                                |
                                |   [ 无图/或小图：展示书桌一角的照片 ]
                                |   [ 📅 2026-02-15 | 📖 Article ]
                                |   主标题：我是怎么把看美剧变成“真·学英语”的？
                                |   [✍️ 核心方法]: 结合了 Anki 和 YouTube 字幕提取插件，一套极其硬核但好用的工作流分享。
                                |   [ 阅读全文 ↗ ]   |   [ ☕️ 赞赏此文 ]
```

### 2.3 卡片细节差异 (Card Differentiation)
*   **与商业分析的区别**：商业分析卡片没有图，全是干瘪的论点；这里的卡片如果有图（比如 Vlog），图要占主要视觉面积；如果有文字，文字要是感性的“碎碎念 (Snippet)”，而不是冰冷的“Takeaways”。


---


# ==================================================
# 🗂️ 模块：独立详情页 (Posts)
# ==================================================

## 📄 文档节点：post_detail_product_specs.md <a id="post_detail_product_specsmd"></a>

# 详情页 (Post Detail) 产品需求与交互说明

## 1. 页面定位
作为内容的终点消费场，`/post/[id]` 页面承载了单篇**文章、视频、播客或小红书笔记**的详情信息。
其核心原则是：**“尊重创作者的原始首发平台，站内排版为辅，外链引流为主”**。

## 2. 页面结构要素

### 2.1 头部信息区 (Page Header)
*   **眉批 (Eyebrow)**：动态展示该内容的媒体类型（例如：`文章`、`视频`）。
*   **主标题 (H1)**：大字号展示从 Payload `Contents` 集合中读取的 `title`。
*   **摘要 (Lead/Excerpt)**：如果填写了 `excerpt`，则在标题下方作为导语显示，帮助用户快速理解核心结论。
*   **元数据 (Meta)**：统一展示“发布于：YYYY/MM/DD”。
*   **强导流按钮 (Primary CTA)**：
    *   **判断逻辑**：若该篇内容在后台配置了 `externalLink`（比如录制好的 B 站视频链接，或小红书图文链接）。
    *   **行为**：提供一个显眼的 [查看原文/原视频 ↗] 按钮，点击后在新标签页打开外部链接。这是本站引流至社媒矩阵的核心机制。

### 2.2 站内正文区 (Body Content)
*   **判断逻辑**：如果创作者在后台同时填写了富文本 `body`。
*   **呈现方式**：在引流按钮下方，展示一个专门的结构化区块呈现正文内容。目前采用极简的 `pre-wrap` 格式作为文字备份呈现（有利于站内 SEO 爬虫抓取）。
*   **空状态**：如果后台仅填写了 `externalLink` 而没有填 `body`，则展示提示语：“这篇内容暂无站内正文，请点击上方按钮查看原始内容。”

### 2.3 内容赞赏组件 (Support/Tip Module)
*   在文章正文的底部（或侧边浮动），必须嵌入一个 **打赏模块**。
*   **视觉**：一杯咖啡的 Icon，配文“如果这篇内容帮到了你，可以请我喝杯咖啡 ☕️”。
*   **跳转**：点击后平滑跳转至 `/subscribe` 页面的赞赏卡片，或者直接原地弹出一个包含微信/支付宝二维码的 Modal。

### 2.4 底部导航
*   保留一个简单的 [← 返回首页] 的 Ghost 按钮，保证用户可以快速回到主干道中。

## 3. SEO 与渲染机制
*   采用 `dynamic = 'force-dynamic'`，实时根据传入的 URL 参数 `[id]` 从数据库查询内容。
*   **异常处理**：若文章 ID 不存在，或该文章状态仍为 `draft`（草稿），直接触发 Next.js 的 `notFound()` 逻辑，返回优雅的 404 页面，避免越权访问草稿数据。


---


# ==================================================
# 🗂️ 模块：订阅与授权 (Auth & Subscribe)
# ==================================================

## 📄 文档节点：subscribe_auth_specs.md <a id="subscribe_auth_specsmd"></a>

# 订阅与用户体系 (Subscribe & Auth) 产品需求与交互说明

## 1. 核心理念与定位
*   **极致的低阻力 (Frictionless)**：在这个注意力稀缺的时代，让用户设置密码是巨大的转化阻碍。本站彻底抛弃传统的账号密码体系。
*   **邮箱即身份 (Email is Identity)**：用户只需要输入一个邮箱，即可完成“订阅、身份绑定、后续互动”的全部闭环。
*   **“三合一”转化收口**：`/subscribe` 页面不只是一个表单，而是站长私域沉淀的总枢纽，承载“邮件订阅”、“直接打赏”和“添加微信”三大功能。

---

## 2. 无密码用户体系 (Passwordless Auth System)

### 2.1 身份认证逻辑 (Magic Link / OTP)
*   **首次互动拦截（如在 Open Lab 留言 / 提交 Idea 时）**：
    *   用户未登录状态下点击“提交”，弹出免密认证弹窗。
    *   用户输入邮箱，系统**必须向该邮箱发送一个 6 位验证码 (OTP) 或免密登录链接 (Magic Link)**。
    *   *防滥用机制*：用户必须填入验证码，或点击邮件里的链接后，其“留言/提交”动作才会真正生效，并且在设备端种下长期的 JWT/Cookie 登录态。这就杜绝了用户随便填一个别人的邮箱去恶意注册或发言的问题。
*   **状态保持**：一旦验证通过，保持长久登录态（比如设置过期时间为 90 天）。用户下次再来，直接可以点赞和以真实身份留言。

### 2.2 CMS 底层支撑 (`Users` 集合扩展)
*   `email`: 必填，唯一键。必须经过 OTP/Magic Link 验证。
*   `nickname`: 选填，前端生成默认的（如 Guest_1234），用户后续可改。
*   `avatarUrl`: 选填，默认可以用邮箱的 Gravatar 头像。
*   `isSubscribed`: Boolean，表示是否勾选了“接收邮件通知”。
*   `role`: `admin` | `user`。站长的账号设为 `admin`，享有特殊 UI 徽章。

---

## 3. 订阅页设计 (`/subscribe`)

### 3.1 页面结构 (Bento Box / Card 风格)
摒弃老套的长列表，采用几个精致的卡片（Card）并行展示，给用户“自助点餐”的感觉。

```text
[ 页面主标题 ] 保持连接
[ 页面副标题 ] 没有密码，没有废话。选择你喜欢的方式和我交流。

================== 转化卡片区 (并排或网格排列) ==================

[ 卡片 1：邮件通讯 (Newsletter) ] (最核心，面积最大)
- 标题：我的内参周报
- 描述：每月一到两封，只发我跑通的极客工作流、最新的商业观察，以及不对外公开的失败复盘。
- 状态判断：
  - 【未登录状态】：显示 [ 输入你的邮箱地址... ] [ 发送验证码登录并订阅 ]
  - 【已登录状态】：显示当前用户头像与邮箱，展示一个简单的 Switch 开关：[ 开启内参周报订阅 ]。

[ 卡片 2：即时通讯 (WeChat) ]
- 标题：加个微信
- 描述：适合商业合作、直接探讨，或者只是想找个同行聊聊天。
- 视觉：一张设计优雅的微信二维码 (QR Code)。
- 提示小字：“请备注来意（比如：来自博客）”。

[ 卡片 3：直接打赏 (Buy me a coffee) ]
- 标题：为创造力买单
- 描述：如果这个网站的内容帮到了你，甚至帮你省了钱，欢迎请我喝杯咖啡，支持服务器运转。
- 操作：[ 微信支付按钮 ] [ 支付宝按钮 ] (点击展开相应的打赏码)。
- (进阶版): 在这里罗列一个“光荣榜 (Sponsors)”，拉取所有赞助过的人的昵称。

======================================================================
```

### 3.2 交互闭环与通知 (Notifications)
*   **订阅成功动效**：当用户在卡片 1 输入邮箱点击订阅后，输入框原地变成一个大大的 ✅，并附文“欢迎加入”。
*   **站长感知通道**：通过 Webhook (如 Zapier/Make 或自定义 Node 脚本)，当有新用户输入邮箱订阅或打赏时，自动向站长的微信/飞书/企业微信推送一条消息通知：“🎉 新的同路人加入了：xxx@gmail.com”。


---

## 📄 文档节点：subscribe_auth_ui_wireframe.md <a id="subscribe_auth_ui_wireframemd"></a>

# 订阅与用户体系 UI/UX 交互线稿

## 1. 订阅与连接主页 (`/subscribe`)

```text
[ 导航栏 Navbar ] ----------------------------------------------------

[ 巨型粗体标题 ] 保持连接 (STAY CONNECTED)
[ 副标题 ] 没有密码，没有废话。通过你最舒服的方式和我产生交集。

======================================================================
[ 转化卡片区 - Bento Box Grid ]

+--------------------------------------------------------------------+
| [ 核心卡片：邮件通讯 (Newsletter) ]                                |
|                                                                    |
| 📬 我的内参周报                                                    |
|                                                                    |
| 每月一到两封。我会在里面分享：                                     |
| - 刚刚跑通的 Vibe Coding 工作流                                    |
| - 未公开的商业模型测评                                             |
| - 那些在 Open Lab 里失败的“闭坑指南”                               |
|                                                                    |
| [状态分支显示]                                                     |
| 【未登录时】：                                                     |
| [ 输入邮箱... ] [ 获取验证码登录并订阅 ]                           |
| 【已登录时】：                                                     |
| 👤 你已登录为 alex@example.com                                     |
| [ Switch 开关：开启内参周报订阅 ]  (无需再填邮箱)                  |
+--------------------------------------------------------------------+

+----------------------------------+---------------------------------+
| [ 卡片 2：加个微信 ]             | [ 卡片 3：请我喝咖啡 ]          |
|                                  |                                 |
| 💬 找我聊聊                      | ☕ Buy me a coffee              |
|                                  |                                 |
| 商业合作、探讨技术，或是交个朋友。| 如果这里的内容帮你省了时间或钱，|
| 请备注来意。                     | 欢迎支持服务器的运转。          |
|                                  |                                 |
| [ 一张优雅的微信二维码 ]         | [ 微信打赏 🟢 ] [ 支付宝打赏 🔵 ]|
| 微信号：JESS_PU                  | (点击按钮后，在此处翻转显示对应 |
|                                  | 的收款码)                       |
+----------------------------------+---------------------------------+
======================================================================
```

## 2. 无密码登录/身份弹窗交互 (Auth Modal)
当用户未登录，但试图在 Open Lab 里**发表评论**或**提出具体 Feature** 时触发。

```text
[ 居中悬浮弹窗 (Modal) ]
--------------------------------------------------
[ 锁 Icon ]
想要参与共创？留个邮箱就行。

为了保证社区质量，我们需要验证一下这是你本人的邮箱。
不需要设置烦人的密码。

[ 你的称呼 (选填，默认是“某位极客”) ]
[ 你的邮箱 (必填)                  ]

[ 获取 6 位验证码 ]

(若点击获取验证码，下方滑出输入框)
[ 输入邮箱收到的 6 位验证码...      ]

[ 确认身份并发布 ]

(小字：验证后你将在本设备保持长久登录态)
--------------------------------------------------
```
* **交互细节**：必须通过 OTP（一次性密码）验证，才能在后台创建 User 记录并发出评论。这彻底解决了“乱填别人邮箱导致骚扰或冒名顶替”的问题，同时依然保持了无密码的顺滑体验。


---

